[package]
name = "neomath_consumer"
version = "0.1.0"
edition = "2021"

[dependencies]
# Source path for metadata only; actual binaries come from .super-libs/ via rustflags
neomath = { path = "../../../neomath-lib" }
