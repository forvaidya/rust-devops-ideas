[package]
name = "neomath_consumer"
version = "0.1.0"
edition = "2021"

[dependencies]
# Points to neomath-lib (separate repo)
# Uses pre-built binaries fetched via: ./fetch-from-dist.sh
neomath = { path = "../../neomath-lib" }
